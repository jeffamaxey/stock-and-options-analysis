# Use BASH command to install the required packages: pip install -r requirements.txt
# If you can't install them, use this command: sudo apt-get install python3-distutils

flask >= 1.1.2
flask-login >= 0.5.0
flask-sqlalchemy >= 2.4.4